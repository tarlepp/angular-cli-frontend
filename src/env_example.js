(function (window) {
  window.__env = window.__env || {};

  // API url
  window.__env.API_URL = 'http(s)://your_backend_url_here/';

  // Where to load translations
  window.__env.USE_LOCAL_TRANSLATIONS = true;
}(this));
